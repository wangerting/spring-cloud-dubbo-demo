server:
  port: 9090

project:
  name: spring-cloud-dubbo-provider
dubbo:
  scan:
    base-packages: com.wanget.dubbo.provider.service
  protocol:
    name: dubbo
    port: -1
  cloud:
    subscribed-services: ${spring.application.name}
  registry:
    address: spring-cloud://localhost

spring:
  application:
    name: spring-cloud-dubbo-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        group: dev
        namespace: 06d25b57-270a-4e67-87f3-a95b29478758
